<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.india.AccidentNotifier">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Places_map"
            android:label="@string/title_activity_places_map"></activity>
        <activity android:name=".Circle_Init.AddPlacesMain" />
        <activity android:name=".Circle_Init.AddPlaces" />
        <activity android:name=".Circle_Init.PermissionPage" />
        <activity android:name=".Circle_Init.PhotoPage" />
        <activity android:name=".InviteCodePage" />
        <activity android:name=".Circle_Init.MakeCircle" />
        <activity android:name=".Circle_Init.JoinCircle" />
        <activity android:name=".Circle_Init.Circle_Initiation" />
        <activity android:name=".SignIn.Create_Password_Page" />
        <activity android:name=".SignIn.Email_Page" />
        <activity android:name=".SignIn.NamePage" />
        <activity android:name=".SignIn.OtpValidationActivity" />
        <activity android:name=".SignIn.Phone_Number_Actvity" />
        <activity android:name=".Login_Page" />
        <activity android:name=".CreateCircle" />
        <activity android:name=".EnterCircle" />
        <activity android:name=".SignIn.Switcher" />
        <activity android:name=".Sign_In_Page" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SignIn.First_Page">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>